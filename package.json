{
  "name": "webhook",
  "version": "1.0.0",  // ✅ Double quotes, no trailing comma
  "description": "Dodo Payment Webhook",
  "main": "index.js",
  "type": "module",    // Remove if using CommonJS (`require`)
  "scripts": {
    "start": "node index.js"  // ✅ No comma here
  },
  "dependencies": {
    "express": "^4.18.2",
    "body-parser": "^1.20.2"  // ✅ No trailing comma
  }
}
